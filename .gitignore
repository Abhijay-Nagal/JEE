# This project has zero runtime dependencies and no build step, so there is
# very little to ignore. node_modules only ever appears if someone installs
# Playwright locally to run `npm run ui`.
node_modules/
npm-debug.log*
.npm/

# Editors
.vscode/
.idea/
*.swp

# OS
.DS_Store
Thumbs.db
desktop.ini

# Scratch
*.local
.env
